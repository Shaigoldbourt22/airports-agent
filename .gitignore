# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Local chat history. data/airports.db is committed on purpose, so the repo
# answers questions on clone; rebuild it with: python etl/build_db.py
data/sessions.db*
